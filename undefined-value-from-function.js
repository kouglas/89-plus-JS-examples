//example
var sum = 0;
function addThree() {
    sum += 3;// if you don't specify the return
    //value then the value will be set to undefined. 
}
function addFive() {
    sum += 5;
}
console.log(addThree());
console.log(addFive());